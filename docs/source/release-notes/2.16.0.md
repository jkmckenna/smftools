(v2.16.0)=

2.16.0 2026-07-22
Version 2.16.0 completes the partitioned experiment/project pipeline with transactional stage manifests, detected resource envelopes and bounded pool admission, immutable HMM model artifacts with deterministic fit/apply planning, project-wide molecule identities, indexed lazy partition queries, and shared original-coordinate genome region planning with cross-core plot stitching. It adds streaming project reducers and transactional partition export, lifecycle-scoped progress and scheduler-visible batch summaries, portable dependency/CI profiles, and synthetic cross-modality, cross-mode, relocation, and resource acceptance coverage. It also fixes genome spatial output without plot regions, avoids redundant spatial reduction work, and skips plot regions with no task coverage instead of failing.
