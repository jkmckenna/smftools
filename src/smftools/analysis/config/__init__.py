"""
smftools.analysis.config — static configuration objects.

No runtime inputs; import and use directly.

Modules
-------
hmm_histogram    HISTOGRAM_CONFIGS  — binning, rolling-mean, peak-calling settings
                                      keyed by HMM feature layer name.
"""
